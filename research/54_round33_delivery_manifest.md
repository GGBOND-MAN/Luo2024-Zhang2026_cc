# Round33 增量交付包说明

日期：2026-09-10

本包是基于当前完整工程的增量审计包，不重复打包全部历史源码或大型 z/Y MAT。运行时需要保持 `source_hashes.csv` 所列的 `+r32/+r31/+r30/+r29/+fsjad/+jad` 基础依赖一致。

包含：

- `matlab/+r33` 全部实际源码；
- Round33 实验、验收、计时、台账、冻结和预检入口；
- 13 项 Round33 专项测试；
- R33 的 v1/v2 验收、复杂度、计时、接受身份、验证和锁定最终候选结果；
- research/52–54；
- 本地文本日志，包括失败和修复后的最终日志。

明确未包含/未执行：

- 不包含 60 用户组合回归结果，因为 Gram/AB 未通过门禁；
- 不包含任何 600/1000/10000 用户新实验；
- 不包含最终测试授权文件；
- 不包含 1400 个最终 trial 结果；
- 不重复包含历史大型原始观测目录。

接受身份：`R33-A-equivalent-implementation-accepted-v1`。最终候选协议仅为待批准状态，不能直接启动最终测试。
